FEELINGS-TURNIER – NPM 404 REPARATUR

Fehlerursache:
GitHub Actions versucht electron-to-chromium 1.5.421 zu laden. Diese Version liefert aktuell 404.

Bitte im Repository ersetzen:
1. package.json
2. .github/workflows/static.yml

Danach Commit auf main.

Der Fix:
- pinnt die transitive Abhängigkeit electron-to-chromium auf 1.5.420
- setzt die npm Registry explizit auf https://registry.npmjs.org/
- behält Node.js 22 und die Node-24-kompatiblen GitHub Actions bei
- benötigt weiterhin keine package-lock.json

Nach dem Commit startet der Pages-Workflow automatisch.
